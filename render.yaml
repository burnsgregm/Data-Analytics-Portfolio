services:
  - type: web
    name: video-annotator-demo
    env: docker
    region: oregon
    branch: main

    # 1) Tell Render where your demo lives:
    directory: FreeFuse_Internship_Project/video-annotator-demo

    # 2) And which Dockerfile in that directory to use:
    dockerfilePath: Dockerfile